{
  "name": "newzelland-whatsapp-bot",
  "version": "1.0.0",
  "description": "Bot de WhatsApp con IA + RAG para Newzeland Cerámicas — proceso independiente (no vive en Vercel)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build-embeddings": "node scripts/build-embeddings.js"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.26.0",
    "qrcode-terminal": "^0.12.0",
    "pg": "^8.11.0",
    "dotenv": "^16.3.0",
    "@anthropic-ai/sdk": "^0.32.0"
  },
  "engines": {
    "node": ">=20"
  }
}
